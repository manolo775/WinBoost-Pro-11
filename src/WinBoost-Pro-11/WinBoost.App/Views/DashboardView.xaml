<UserControl x:Class="WinBoost.App.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:WinBoost.App.Controls"
             mc:Ignorable="d"
             d:DesignHeight="800"
             d:DesignWidth="900">

	<ScrollViewer
        VerticalScrollBarVisibility="Auto"
        HorizontalScrollBarVisibility="Disabled">

		<Grid
            Margin="0,0,0,30"
            Background="#1E1E1E">

			<Grid.RowDefinitions>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="Auto"/>
			</Grid.RowDefinitions>

			<!-- TITLU -->

			<TextBlock
                Grid.Row="0"
                Text="Dashboard"
                FontSize="32"
                FontWeight="Bold"
                Foreground="White"
                Margin="40,20,40,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"/>

			<!-- SYSTEM HEALTH -->

			<controls:HealthCard
                Grid.Row="1"
                Margin="50,25,50,5"/>

			<!-- CARDURI SISTEM -->

			<UniformGrid
                Grid.Row="2"
                Rows="2"
                Columns="2"
                Margin="40,10,40,10">

				<controls:MetricCard
                    IconKind="Cpu64Bit"
                    Title="CPU Usage"
                    Value="{Binding CpuUsage}"
                    Status="{Binding CpuStatus}"
                    ProgressValue="{Binding CpuUsageValue}"
                    Margin="10"/>

				<controls:MetricCard
                    IconKind="Memory"
                    Title="RAM Usage"
                    Value="{Binding RamUsage}"
                    Subtitle="{Binding RamDetails}"
                    Status=""
                    ProgressValue="{Binding RamUsageValue}"
                    Margin="10"/>

				<controls:MetricCard
                    IconKind="Harddisk"
                    Title="Disk Usage"
                    Value="{Binding DiskUsage}"
                    Status=""
                    ProgressValue="{Binding DiskUsageValue}"
                    Margin="10"/>

				<controls:MetricCard
                    IconKind="ClockOutline"
                    Title="Windows Uptime"
                    Value="{Binding Uptime}"
                    Subtitle=""
                    Status=""
                    ProgressValue="0"
                    ShowProgressBar="False"
                    Margin="10"/>

			</UniformGrid>

			<!-- QUICK ACTIONS -->

			<controls:QuickActionsControl
                Grid.Row="3"
                Margin="50,15,50,0"/>

		</Grid>

	</ScrollViewer>

</UserControl>